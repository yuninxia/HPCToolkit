{
  "capAdd": [
    "SYS_ADMIN",
    "PERFMON"
  ],
  "containerEnv": {
    "LANG": "${localEnv:LANG:C.UTF-8}",
    "OMPI_ALLOW_RUN_AS_ROOT": "1",
    "OMPI_ALLOW_RUN_AS_ROOT_CONFIRM": "1"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "mesonbuild.mesonbuild"
      ],
      "settings": {
        "mesonbuild.buildFolder": "builddir",
        "mesonbuild.configureOnOpen": true
      }
    }
  },
  "image": "registry.gitlab.com/hpctoolkit/rtt-images:1.0.0-aio",
  "mounts": [
    {
      "source": "hpctoolkit-builddir-${devcontainerId}",
      "target": "${containerWorkspaceFolder}/builddir",
      "type": "volume"
    },
    {
      "source": "ccache-${devcontainerId}",
      "target": "/var/cache/ccache",
      "type": "volume"
    },
    {
      "target": "${containerWorkspaceFolder}/.cache",
      "type": "volume"
    }
  ],
  "onCreateCommand": {
    "compile_commands.json": "[ -e '${containerWorkspaceFolder}'/compile_commands.json ] && [ \"$(readlink '${containerWorkspaceFolder}'/compile_commands.json)\" = builddir/compile_commands.json ] || ln -sb builddir/compile_commands.json '${containerWorkspaceFolder}'/compile_commands.json"
  },
  "remoteEnv": {
    "CCACHE_DIR": "/var/cache/ccache"
  }
}
